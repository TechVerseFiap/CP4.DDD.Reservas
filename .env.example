# Local Docker Compose credentials only.
ORACLE_SYS_PASSWORD=OracleSysDev_12345
ORACLE_DB_USERNAME=RESERVAS
ORACLE_DB_PASSWORD=ReservasDev_12345

# Used when the API runs directly on the host. Compose overrides this with the
# Oracle service hostname.
ORACLE_DB_URL=jdbc:oracle:thin:@localhost:1521/XEPDB1

ORACLE_PORT=1521
API_PORT=8080
